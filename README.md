# Object compare

This repo shallowly compares two objects and displays them in a console window

## Sample

Default sample comparision of `DateTime` and `DateTimeOffset` ;

![sample-image](./images/sample.png)
